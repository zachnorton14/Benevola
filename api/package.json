{
  "dependencies": {
    "@elastic/elasticsearch": "^9.2.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "nodemon": "^3.1.11",
    "sequelize": "^6.37.7",
    "sqlite3": "^5.1.7",
    "zod": "^4.2.1"
  },
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:migrate": "sequelize-cli db:migrate --env development",
    "db:seed": "sequelize-cli db:seed:all --env development",
    "db:fresh": "rm -f data/dev.sqlite && sequelize-cli db:migrate --env development && sequelize-cli db:seed:all --env development"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "sequelize-cli": "^6.6.3"
  }
}
